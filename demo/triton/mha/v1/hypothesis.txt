Hypothesis: Add query-side tiling (BLOCK_I=64), use tl.dot for QK^T and PV matmuls
Rationale : v1 still Memory-Bound (Memory SOL=90.46% >> SM SOL=26.75%). WarpExecEff=32%, no Tensor Core usage. Each program processes only 1 query — no K/V reuse across queries. Past experience (id=27) confirms 13.6x speedup.
Expected  : Memory SOL may drop as compute becomes the bottleneck. Tensor Core utilization > 40%. Latency ↓ > 10x. WarpExecEff may improve.
